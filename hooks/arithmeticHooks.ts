// Arithmetic Hooks


export function useMultiply(num1: number, num2: number) {
  const value = num1 * num2 ;

  return value
}

export function useAdd(num1: number, num2: number) {
  const value = num1 + num2 ;

  return value
}

